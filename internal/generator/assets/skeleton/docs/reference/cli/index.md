# CLI Reference

Command reference for {{ .Name }}. Run `{{ .Name }} <command> --help` for the
authoritative, always-current flag set.

_Command pages are generated by `gtb generate docs --command <name>`._
