// Package gitea provides a release.Provider implementation for Gitea and
// Forgejo instances, including Codeberg (codeberg.org). Provider construction
// uses package-owned [Settings]; GTB config integration lives in
// [SettingsFromConfig].
package gitea
