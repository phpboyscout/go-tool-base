version: 2
project_name: gtb
force_token: gitlab
before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: gtb-darwin
    main: ./cmd/gtb
    env:
      - CGO_ENABLED=0
      - GOLANG_FIPS=1
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X gitlab.com/phpboyscout/go-tool-base/internal/version.version={{.Version}} -X gitlab.com/phpboyscout/go-tool-base/internal/version.commit={{.Commit}} -X gitlab.com/phpboyscout/go-tool-base/internal/version.date={{.Date}}
      - -X gitlab.com/phpboyscout/go-tool-base/internal/cmd/root.otelAuth={{.Env.GTB_OTEL_AUTH}}
  - id: gtb
    main: ./cmd/gtb
    env:
      - CGO_ENABLED=0
      - GOLANG_FIPS=1
    goos:
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X gitlab.com/phpboyscout/go-tool-base/internal/version.version={{.Version}} -X gitlab.com/phpboyscout/go-tool-base/internal/version.commit={{.Commit}} -X gitlab.com/phpboyscout/go-tool-base/internal/version.date={{.Date}}
      - -X gitlab.com/phpboyscout/go-tool-base/internal/cmd/root.otelAuth={{.Env.GTB_OTEL_AUTH}}

notarize:
  macos:
    - enabled: '{{ isEnvSet "APPLE_DEV_CERT" }}'
      ids:
        - gtb-darwin
      sign:
        certificate: "{{.Env.APPLE_DEV_CERT}}"
        password: "{{.Env.APPLE_DEV_CERT_PASSWORD}}"
      notarize:
        issuer_id: "{{.Env.APPLE_NOTARY_ISSUER_ID}}"
        key_id: "{{.Env.APPLE_NOTARY_KEY_ID}}"
        key: "{{.Env.APPLE_NOTARY_KEY}}"

archives:
  # Single archive covering both darwin and linux/windows builds.
  # The OS keychain backend now lives in its own module
  # (pkg/credentials/keychain) that cmd/gtb does not import, so the
  # shipped binary uses the stub credentials backend and performs no
  # session-bus / keychain IPC — the regulated-deployment default.
  # Downstream tools that want keychain support import the subpackage
  # in their own cmd package.
  - formats: ["tar.gz"]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
sboms:
  - artifacts: archive

# Pin the checksums manifest filename so pkg/setup's Update() path
# finds it via the default asset-list lookup. GoReleaser v2's default
# is "{{ .ProjectName }}_{{ .Version }}_checksums.txt", which the
# spec's default lookup name ("checksums.txt") does not match.
# docs/development/specs/2026-04-02-remote-update-checksum-verification.md
# describes the contract.
checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# Detached, ASCII-armored OpenPGP signature over checksums.txt
# (-> checksums.txt.sig), consumed by Phase 2 self-update signature
# verification in pkg/setup. Dormant until a signing key is provisioned:
# the release job runs goreleaser with `--skip=sign` unless
# GTB_SIGNING_KEY is set (see .gitlab-ci.yml), mirroring the notarize
# gate on APPLE_DEV_CERT. See docs/development/phase2-signing-prep.md.
signs:
  - id: checksums
    cmd: scripts/sign-release.sh
    artifacts: checksum
    signature: "${artifact}.sig"
    args: ["${artifact}", "${signature}"]
    output: true

release:
  use_existing_draft: true
  replace_existing_artifacts: true
  make_latest: true
  # keep-existing: releaser-pleaser owns the Release notes (it creates
  # the Release with its changelog when the Release MR is merged). This
  # job only attaches/replaces the build artefacts — it must not
  # overwrite the body.
  mode: keep-existing
changelog:
  # releaser-pleaser owns the changelog (CHANGELOG.md and the Release
  # notes), so GoReleaser does not generate its own.
  disable: true
gitlab_urls:
  api: https://gitlab.com/api/v4/
  download: https://gitlab.com/
  use_package_registry: true
  use_job_token: true
  skip_tls_verify: false
snapshot:
  version_template: "{{ incpatch .Version }}-dev{{.ShortCommit}}"

# Homebrew cask published to the GitLab-hosted tap at
# gitlab.com/phpboyscout/homebrew (mirrored from the previous GitHub
# tap during the GitLab migration). Auth via HOMEBREW_TAP_GITLAB_TOKEN
# CI variable scoped to the tap repo only — a separate token from
# GITLAB_TOKEN so a compromise of one cannot cross to the other.
homebrew_casks:
  - name: gtb
    binary: gtb
    conflicts:
      - formula: gtb
    commit_author:
      name: goreleaserbot
      email: matt@phpboyscout.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    homepage: "https://gitlab.com/phpboyscout/go-tool-base"
    description: "A helper utility for interacting and managing gtb repos and resources"
    dependencies:
      - formula: go
      - formula: golangci-lint
    repository:
      owner: phpboyscout
      name: homebrew
      branch: main
      # GoReleaser has no native GitLab API path for homebrew tap
      # pushes (the `token:` field is GitHub-only). Push over SSH
      # using a deploy key registered on the tap repo with
      # write_access. The private key lives in the
      # HOMEBREW_TAP_SSH_KEY masked CI variable.
      git:
        url: "git@gitlab.com:phpboyscout/homebrew.git"
        private_key: "{{ .Env.HOMEBREW_TAP_SSH_KEY }}"
