# Configuration Reference

Configuration keys {{ .Name }} reads, with types, defaults, and the
environment-variable mapping.

_Document each key as `<section>.<key>` with its type, default, and meaning._
