# Changelog

All notable changes to this project will be documented in this file.

This changelog is automatically generated from conventional commits during the
release process via `go tool changelog generate`.

To view the changelog from the CLI, run:

```bash
{{ .Name }} changelog
{{ .Name }} changelog --latest
{{ .Name }} changelog --version v1.0.0
```
